'use client';

// The map page is intentionally empty.
// The map, controls, and side panel are rendered by the map layout.
// This page exists to provide the /map route.

export default function MapPage() {
  return null;
}
